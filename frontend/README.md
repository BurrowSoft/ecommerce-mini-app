# Frontend

Next.js (App Router) frontend for the e-commerce mini-app. See the
[repo-root README](../README.md) for setup, architecture, and everything else —
this file intentionally doesn't duplicate it.

Local dev: `npm install && npm run dev` (expects the backend running at
`NEXT_PUBLIC_API_URL`, see `../.env.example`).
